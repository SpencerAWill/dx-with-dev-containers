# Copy to .env and point at whatever the phone can reach.
# For tunnel mode + VS Code public port forwarding, use that public URL.
# For LAN mode, use http://<host-lan-ip>:5000
EXPO_PUBLIC_API_URL=http://localhost:5000
