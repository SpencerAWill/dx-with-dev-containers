{
  "name": "snapsort",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "web": "pnpm --filter web-app dev",
    "web:build": "pnpm --filter web-app build",
    "mobile": "pnpm --filter mobile start",
    "mobile:tunnel": "scripts/mobile-tunnel.sh",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
