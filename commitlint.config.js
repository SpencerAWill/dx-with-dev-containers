/**
 * Conventional Commits, enforced on the commit message.
 *
 * The history was already written this way by hand; this makes it a rule
 * rather than a habit, so it survives contributors who did not write the
 * earlier commits.
 */
export default { extends: ["@commitlint/config-conventional"] };
